apiVersion: redis.managed.com/v1
kind: ManagedRedis
metadata:
  name: whchoi-redis
spec:
  version: "7.0.0"
  replicas: 2
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "200m" 